dist
node_modules
data
coverage
package-lock.json

# Prose is not formatted: Prettier's table alignment miscounts accented characters
# and rewrites documents for no benefit. Markdown is reviewed by humans, not tools.
*.md
